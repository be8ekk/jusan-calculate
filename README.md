# Task 

``` 
У вас есть класс Calculate, в котором есть приватный массив целых чисел.

Создайте следующее:

1) AdditionProtocol - протокол имеющий метод, который прибавляет все элементы массива

2) MultiplicationProtocol - протокол имеющий метод, который умножает все элементы массива

3) Расширение протокола MultiplicationProtocol, который добавляет новую функциональность в виде метода сортировки.

4) Напишите тестовый код для демонстрации
```